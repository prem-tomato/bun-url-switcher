{
  "name": "url-switcher-backend",
  "version": "1.0.0",
  "description": "Backend API for URL Switcher with CRUD operations using Neon PostgreSQL",
  "main": "api/server.ts",
  "scripts": {
    "dev": "bun run --watch api/server.ts",
    "start": "node dist/server.js",
    "build": "tsc",
    "vercel-build": "npm run build",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "elysia",
    "postgresql",
    "neon",
    "drizzle",
    "api",
    "url-switcher"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@elysiajs/cors": "^1.0.0",
    "@elysiajs/node": "^1.3.0",
    "@vercel/postgres": "^0.10.0",
    "dotenv": "^17.2.1",
    "elysia": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}